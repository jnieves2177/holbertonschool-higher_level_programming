"""0-add_integer.py

This module contains a function for adding two integers or floats.

"""

def add_integer(a, b=98):
    """Add two integers or floats and return the result as an integer.

    Args:
        a (int or float): The first number.
        b (int or float, optional): The second number. Defaults to 98.

    Returns:
        int: The addition of a and b, cast to an integer.

    Raises:
        TypeError: If a or b is not an integer or float.

    Examples:
        >>> add_integer(5, 7)
        12
        >>> add_integer(3.5, 2)
        5
        >>> add_integer(10.8, 5.3)
        16

    """
    # Test

if __name__ == "__main__":
    import doctest
    doctest.testmod()

    # Additional manual test cases
    try:
        # Test valid integer inputs
        result1 = add_integer(5, 7)
        assert result1 == 12

        # Test float inputs that need to be cast to integers
        result2 = add_integer(3.5, 2)
        assert result2 == 5

        result3 = add_integer(10.8, 5.3)
        assert result3 == 16

        # Test float input and default value for b
        result4 = add_integer(3.7)
        assert result4 == 3

        # Test invalid inputs (should raise TypeError)
        add_integer("string", 2)

    except AssertionError:
        print("Test case failed.")
    else:
        print("All test cases passed."

